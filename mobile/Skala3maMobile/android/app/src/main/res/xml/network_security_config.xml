<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Trust user/system CAs for debug to support local HTTPS with self-signed CA -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </debug-overrides>

    <!-- Explicitly allow cleartext HTTP to Metro (10.0.2.2:8081) and localhost for dev -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Pin our local dev certificate for HTTPS to avoid hostname/CN mismatches with self-signed certs. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <trust-anchors>
            <certificates src="@raw/dev_local" />
            <certificates src="user" />
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
